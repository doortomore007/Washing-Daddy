import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.washingdaddy.app',
  appName: 'Washing Daddy',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
